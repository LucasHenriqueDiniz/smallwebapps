node_modules/
.astro/
dist/
.DS_Store
coverage/
package-lock.json
yarn.lock
.env
.env.*
.logs/
.audit-tmp/
smallwebapps.com-audit/

# Claude Code's local permission file. Never commit it: allow-rules accumulate by
# clicking, and a rule that allowlists a command with a token in it puts that token
# in the repo.
**/.claude/settings.local.json

# Generated by scripts/prepare-tubetrace-embed.mjs on every `pnpm build`, which
# runs `pnpm sync:tubetrace` first. The filenames are content-hashed, so keeping
# these tracked meant every build rewrote tracked files with a new name.
apps/web/public/tubetrace-app/

# Derived from docs/pitches and docs/plans by the hexagram board skill. Rebuilt by
# one sync command, and a tracked JSON board is a merge conflict waiting to happen.
.kanban.json
