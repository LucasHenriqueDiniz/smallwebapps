# @smallwebapps/config

Shared configuration lives here when multiple workspace packages need the same setup. The first version includes the shared Astro-oriented TypeScript settings used by the web app.
